mutation($username:ID!,$password:String!,$role:Role!,$firstName:String,$lastName:String,$phone:String){
   register(user:{username:$username,password:$password,role:$role,firstName:$firstName,lastName:$lastName,phone:$phone}){
       status
       msg
   }
}