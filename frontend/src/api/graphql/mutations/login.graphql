mutation($username:ID!,$password:String!,$role:Role!){
   login(username:$username,password:$password,role:$role){
       status
       msg
       token
       data{
           username
       }
   }
}