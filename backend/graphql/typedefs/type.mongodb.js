const typeMongodb = `#related to mongodb
type updateRes{
    acknowledged: Boolean
    modifiedCount: Int
    matchedCount: Int
    upsertedId: ID
    upsertedCount: Int
}`;
export default typeMongodb;
