const typeCourses = `#related to courses
type Course {
    id: ID!
    title: String!
    description: String
    price: Float!
    imageLink: String
    published: Boolean!
    createdAt: String!
    creator: ID!
  }`;

export default typeCourses;
